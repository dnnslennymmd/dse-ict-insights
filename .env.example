# Optional: Mansa API key for live DSE market data
# Without this, the app uses deterministic synthetic seed data for development
MANSA_API_KEY=

# SQLite database path (default: apps/web/data/dse.db)
DATABASE_PATH=
